export const metadata = {
title: "Growtopia Player Monitor (CREATED BY JANE)",
description: "CREATED BY JANE U CAN USE BY GET API",
};

export default function RootLayout({ children }) {
return (
<html lang="en">
<body style={{ margin: 0 }}>{children}</body>
</html>
);
}
